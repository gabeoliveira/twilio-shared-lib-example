{
  "name": "twilio-shared-lib-example",
  "version": "1.0.0",
  "scripts": {
    "build:lib": "esbuild my-shared-lib/index.js --bundle --platform=node --target=node16 --outfile=assets/my-shared-lib.js",
    "deploy": "twilio serverless:deploy"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  },
  "dependencies": {
    "uuid": "^11.1.0"
  }
}
